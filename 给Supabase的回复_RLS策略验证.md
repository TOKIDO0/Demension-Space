感谢您执行了 RLS 策略！

请帮我完成以下验证和配置：

1. **验证当前用户的 role**：
   请帮我检查当前用户在 `public.user_profiles` 表中的 `role` 字段是否为 `admin`。
   如果不是 `admin`，请帮我将当前用户的 `role` 设置为 `admin`。

2. **确认策略生效**：
   如果可以的话，请帮我列出 `public.reviews` 表当前的所有 RLS 策略，确认策略已正确创建。

3. **测试查询**：
   确认我的用户角色为 `admin` 后，请帮我测试一下能否查询到所有评价（包括 `is_hidden = true` 的记录）。

谢谢！
