我选择将第一个用户（id: `296a0f40-fedd-4f2e-9a02-25fb3c8c4ce4`，username: `1810266895@qq.com`）的 `role` 字段更新为 `admin`。

这个用户已经有 `is_admin = true`，但 `role` 字段还是 `user`，请帮我将它的 `role` 更新为 `admin`。

更新完成后，请帮我验证一下：
1. 确认该用户的 `role` 已成功更新为 `admin`
2. 测试一下该用户能否查询到所有评价（包括 `is_hidden = true` 的记录）

谢谢！

